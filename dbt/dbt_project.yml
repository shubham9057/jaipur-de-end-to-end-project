## Project
name: de_project
version: "1.0"
config-version: 2
profile: de_project

## Sources
model-paths: [models]
analysis-paths: [analysis]
test-paths: [tests]
seed-paths: [seeds]
macro-paths: [macros]
snapshot-paths: [snapshots]

## Target
target-path: target
clean-targets: [target, dbt_modules]

## Models
models:
  de_project:
    staging:
      +materialized: view
    marts:
      +materialized: table